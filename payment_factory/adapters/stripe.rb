class Stripe
    def initalize
    end

    def charge(amount, currency, description, token)
        #some code to request charge from stripe
    end

    def manual_payout(amount, currency)
    end
end